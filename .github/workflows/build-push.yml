name: Frontend — Build, Test & Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

env:
  REGISTRY_NS: tracybachmann
  TAG: latest

concurrency:
  group: prod-deploy-frontend
  cancel-in-progress: false

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          # Nettoyage du cache en cas de problème
          npm cache clean --force || true
          npm ci --no-audit --no-fund

      - name: Lint
        run: npm run lint || echo "⚠️ Pas de lint configuré, on continue..."

      - name: Build Angular
        run: npm run build

      - name: Run tests
        run: npm test -- --watch=false --browsers=ChromeHeadless || echo "⚠️ Pas de tests configurés, on continue..."

  build-push-frontend:
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: docker.io/${{ env.REGISTRY_NS }}/backend-frontend
          tags: |
            type=raw,value=latest
            type=sha,prefix=sha-,format=long
            type=ref,event=branch

      - name: Build & Push frontend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          no-cache: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILDKIT_INLINE_CACHE=1

  deploy:
    name: Deploy frontend to VM
    needs: build-push-frontend
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Deploy & Health Check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script_stop: true
          command_timeout: 10m
          envs: DOCKERHUB_USER,DOCKERHUB_TOKEN
          script: |
            set -e
            cd /opt/bibliomate

            echo "🔐 Login Docker Hub..."
            docker login -u "$DOCKERHUB_USER" -p "$DOCKERHUB_TOKEN"

            echo "📥 Pull nouvelle image frontend..."
            docker compose --env-file .env -f deploy/docker-compose.prod.yml pull frontend

            echo "🔄 Redémarrage frontend..."
            docker compose --env-file .env -f deploy/docker-compose.prod.yml up -d frontend

            echo "⏳ Attente démarrage (10s)..."
            sleep 10

            echo "🏥 Health check..."
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1/ || echo "000")

            if [ "$HTTP_CODE" = "200" ]; then
              echo "✅ Déploiement réussi ! Frontend opérationnel."
              docker compose -f deploy/docker-compose.prod.yml ps | grep frontend
              exit 0
            else
              echo "❌ Health check échoué : HTTP $HTTP_CODE"
              echo "📋 Logs frontend:"
              docker logs --tail 50 frontend
              exit 1
            fi
        env:
          DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USER }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Deployment Success
        if: success()
        run: |
          echo "🎉 Frontend déployé avec succès !"
          echo "🔗 https://bibliomate.fr"
          echo "📊 Commit: ${{ github.sha }}"

      - name: Deployment Failed
        if: failure()
        run: |
          echo "❌ Le déploiement a échoué"
          echo "📋 Consulte les logs ci-dessus pour plus de détails"
